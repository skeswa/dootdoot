# Rust build artifacts
/target/
/node_modules/
/docs/.vitepress/cache/
/docs/.vitepress/dist/

# Coverage output (cargo-llvm-cov)
*.profraw
/lcov.info

# Stray render artifacts (e.g. a rejected CLI test's -o target that should never be written)
ignored.wav

# macOS
.DS_Store

# Note: Cargo.lock is intentionally committed (this is an application; see docs/style.md §12).
